fprintf(stderr, "%d\n", nips);

//for (int pi=0; pi<nips; pi++) {
    /* m_by_r(pi) = 0; */
    /* for (int j=0; j < 3; j++) m_by_r2(pi,j) = 0; */
    /* for (int i=0; i<n; i++) { */
    /*     //if ((i % 10000) == 0) printf("i: %d\n",i); */
    /*     double mass = mass(i); */
    /*     double dx = ipos(pi,0) - pos(i,0); */
    /*     double dy = ipos(pi,1) - pos(i,1); */
    /*     double dz = ipos(pi,2) - pos(i,2); */
    /*     double drsoft = sqrt(dx*dx + dy*dy + dz*dz + epssq); */
    /*     m_by_r(pi) += mass / drsoft; */
    /*     m_by_r2(pi,0) += mass / drsoft/drsoft/drsoft * dx; */
    /*     m_by_r2(pi,1) += mass / drsoft/drsoft/drsoft * dy; */
    /*     m_by_r2(pi,2) += mass / drsoft/drsoft/drsoft * dz; */
    /*     } */
    /* } */
